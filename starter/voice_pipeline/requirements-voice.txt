# Ex8 voice mode — optional dependencies.
#
# Install with: uv pip install -r starter/voice_pipeline/requirements-voice.txt
# Or: uv sync --extra voice  (uses the [voice] extra in pyproject.toml)
#
# Text mode does NOT need any of these.

speechmatics-rt
speechmatics-tts
elevenlabs >= 0.2.0
sounddevice >= 0.4.6
numpy >= 1.26
